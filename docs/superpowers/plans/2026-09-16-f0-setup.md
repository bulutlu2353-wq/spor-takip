# F0 — Repo & Ortam Kurulumu Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Flutter/Supabase geliştirme ortamını kurmak ve `PLAN.md`'deki E1-E9 epic'lerinin üzerine inşa edileceği çalışan, analiz/test'i yeşil bir proje iskeleti oluşturmak.

**Architecture:** İki paralel kol: (1) OpenCode CLI arka planda ağır SDK kurulumunu yapar (Flutter SDK, Android SDK, JDK 17 — hepsi D: sürücüsüne, C:'de yer kalmadığı için), (2) Claude Code SDK hazır olduğunda `flutter create` ile projeyi oluşturur, feature-first klasör yapısını kurar, Riverpod/Supabase bağımlılıklarını ekler, `.env` tabanlı config ve CI pipeline'ını yazar.

**Tech Stack:** Flutter/Dart, Riverpod, supabase_flutter, flutter_dotenv, go_router, GitHub Actions.

**Spec:** `docs/superpowers/specs/2026-09-16-f0-setup-design.md`

## Global Constraints

- C: sürücüsünde yalnızca ~800MB boş alan var — **hiçbir SDK/araç C:'ye kurulmayacak**, hepsi D: sürücüsüne (`D:\flutter`, `D:\android-sdk`, `D:\jdk17`).
- Proje kök dizini: `D:\spor ugulaması` (dizin adında boşluk ve Türkçe karakter var — tüm komutlarda tırnak kullanılacak).
- Dart paket adı geçerli bir tanımlayıcı olmalı: **`spor_takip`** (proje kök dizin adı geçersiz olduğu için `flutter create --project-name spor_takip` kullanılacak).
- Git deposu zaten `git init` edilmiş, local identity ayarlı (`user.email=bulutlu.2353@gmail.com`), global git config'e dokunulmayacak — her git komutunda `-c safe.directory="D:/spor ugulaması"` kullanılacak.
- Hedef platformlar: Android + iOS (iOS gerçek build/test bu fazda yapılmaz, sadece kod platformu destekleyecek şekilde oluşturulur).
- Klasör mimarisi: feature-first (`lib/features/{onboarding,nutrition,workout,chat,progress,settings}`, her biri `presentation/application/domain/data` alt klasörleriyle).
- `.env` asla commit edilmeyecek; `.env.example` commit edilecek.

---

## Task 1: OpenCode ile SDK kurulumunu arka planda başlat

**Files:** Yok (sadece komut çalıştırma + log dosyası: `$TEMP/opencode_f0_install.log`)

**Interfaces:**
- Consumes: Yok (önceki OpenCode oturumu `ses_f5e5f41deffepxWz7TFMKyLtul`, sadece devam ettiriliyor)
- Produces: Kurulum tamamlandığında sistemde `flutter` ve `dart` komutları PATH'te, `ANDROID_HOME`/`ANDROID_SDK_ROOT` ortam değişkenleri `D:\android-sdk`'ya işaret ediyor, `JAVA_HOME` (kullanıcı-scope) `D:\jdk17`'ye işaret ediyor. Task 2 bu çıktıyı doğrular.

- [ ] **Step 1: Mevcut durumu doğrula (kurulum gerektiğini teyit et)**

Run: `flutter --version`
Expected: `flutter : The term 'flutter' is not recognized...` (henüz kurulu değil)

- [ ] **Step 2: OpenCode'u arka planda, önceki oturumu devam ettirerek başlat**

Şu komutu **arka planda** çalıştır (Bash tool kullanılıyorsa `run_in_background: true` ile; bu görev büyük dosya indirmeleri içerdiği için 10-20 dakika sürebilir):

```bash
opencode run -s ses_f5e5f41deffepxWz7TFMKyLtul "F0 kurulumuna devam et. Önceki adımda Flutter SDK indirmesi kesintiye uğramıştı (%TEMP%\\flutter_stable.zip yarım/geçersiz, sil ve yeniden indir). Şunları yap: (1) En son stable Flutter SDK'yı D:\\flutter dizinine indir ve kur, kullanıcı PATH'ine D:\\flutter\\bin ekle (sadece kullanıcı PATH, sistem PATH'ine dokunma). (2) Android command-line tools + platform-tools + en güncel stable platform ve build-tools paketlerini D:\\android-sdk dizinine kur, ANDROID_HOME ve ANDROID_SDK_ROOT ortam değişkenlerini (kullanıcı-scope) D:\\android-sdk olarak ayarla, Android SDK lisanslarını otomatik kabul et (sdkmanager --licenses için batch onay). (3) JDK 17'yi indirip D:\\jdk17 dizinine kur, JAVA_HOME'u (kullanıcı-scope) D:\\jdk17'ye ayarla — mevcut sistem Java 8 kurulumuna dokunma. (4) C: sürücüsünde sadece ~800MB boş alan var, geçici indirme dosyaları hariç HİÇBİR ŞEYİ C:'ye kurma/yazma; işin bitince geçici indirme dosyalarını (zip'ler) sil. (5) Son olarak 'flutter doctor -v' çalıştır ve tam çıktısını yazdır. Her adımda ilerlemeyi kısaca raporla. En sonunda net bir şekilde 'KURULUM TAMAMLANDI' veya 'HATA: <sebep>' ile bitir." > "$TEMP/opencode_f0_install.log" 2>&1
```

Expected: Komut arka planda çalışmaya başlar, terminal hemen geri döner (bloklamaz).

- [ ] **Step 3: Arka plan görevinin başladığını doğrula**

Run: `sleep 5 && cat "$TEMP/opencode_f0_install.log" | head -20` (veya Bash tool `run_in_background` kullanıldıysa ilk bildirim/log çıktısını kontrol et)
Expected: Log dosyasında OpenCode'un çalışmaya başladığına dair çıktı (örn. indirme mesajları) var, hata yok.

---

## Task 2: SDK kurulumunun tamamlanmasını doğrula

**Files:** Yok

**Interfaces:**
- Consumes: Task 1'in ürettiği `flutter`/`dart` PATH girişleri, `ANDROID_HOME`, `JAVA_HOME`
- Produces: Doğrulanmış Flutter sürüm numarası (Task 7'de CI workflow'una yazılacak) ve çalışan bir Flutter kurulumu (Task 3+ bunu tüketir)

- [ ] **Step 1: Log dosyasını "KURULUM TAMAMLANDI" veya "HATA:" için periyodik kontrol et**

Run: `grep -E "KURULUM TAMAMLANDI|HATA:" "$TEMP/opencode_f0_install.log"`
Expected: Sonunda `KURULUM TAMAMLANDI` satırı görünür. `HATA:` görünürse, log'un tamamını oku, sorunu teşhis et (örn. disk alanı, ağ hatası) ve Task 1'i düzeltilmiş bir talimatla yeniden çalıştır.

- [ ] **Step 2: Yeni terminal/shell ile PATH güncellemesini yükle ve doğrula**

Run: `flutter --version`
Expected: `Flutter X.Y.Z • channel stable ...` şeklinde bir sürüm çıktısı (hata yok). **Bu sürüm numarasını not al — Task 7'de CI workflow'una yazılacak.**

- [ ] **Step 3: Flutter doctor ile tüm bileşenleri doğrula**

Run: `flutter doctor -v`
Expected: "Flutter", "Android toolchain" satırlarında ✓ işareti (kırmızı ✗ olmamalı). Lisans onayı bekliyorsa: `flutter doctor --android-licenses` çalıştır ve tümünü `y` ile onayla.

- [ ] **Step 4: Disk alanının hâlâ güvenli olduğunu doğrula**

Run: `powershell -Command "Get-PSDrive C | Select-Object Free"`
Expected: C: sürücüsünde en az 500MB boş alan kaldığı teyit edilir (kurulum yanlışlıkla C:'ye yazmadı).

---

## Task 3: Flutter proje iskeletini oluştur

**Files:**
- Create: `pubspec.yaml`, `lib/main.dart`, `test/widget_test.dart`, `android/`, `ios/` (hepsi `flutter create` tarafından üretilir)
- Modify: `.gitignore` (flutter create tarafından oluşturulur, Task 6'da `.env` eklenecek)

**Interfaces:**
- Consumes: Task 2'de doğrulanan çalışan `flutter` komutu
- Produces: Paket adı `spor_takip` (Task 5+'daki tüm import'lar `package:spor_takip/...` kullanacak)

- [ ] **Step 1: Proje kök dizininde flutter create çalıştır**

Run:
```bash
cd "D:/spor ugulaması" && flutter create --project-name spor_takip --org com.sportakip --platforms android,ios --description "Spor ve beslenme takip uygulaması" .
```
Expected: `Creating project .../spor ugulaması...` ile başlayıp `Wrote X files.` ile bitiyor, hata yok. Mevcut `PLAN.md`, `docs/`, `.git` dosyalarına dokunulmaz.

- [ ] **Step 2: Varsayılan sayaç uygulamasının çalıştığını doğrula (analyze)**

Run: `flutter analyze`
Expected: `No issues found!`

- [ ] **Step 3: Varsayılan widget testinin geçtiğini doğrula**

Run: `flutter test`
Expected: `00:0X +1: All tests passed!`

- [ ] **Step 4: Commit**

```bash
cd "D:/spor ugulaması"
git -c safe.directory="D:/spor ugulaması" add -A
git -c safe.directory="D:/spor ugulaması" commit -m "$(cat <<'EOF'
Scaffold Flutter project with flutter create

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01PDcH7HBWBdAAUWM5Tnp7GU
EOF
)"
```
Expected: Commit başarılı, `git status` "nothing to commit" gösterir.

---

## Task 4: Feature-first klasör yapısını kur

**Files:**
- Create: `lib/core/.gitkeep`, `lib/shared/.gitkeep`
- Create: `lib/features/onboarding/{presentation,application,domain,data}/.gitkeep`
- Create: `lib/features/nutrition/{presentation,application,domain,data}/.gitkeep`
- Create: `lib/features/workout/{presentation,application,domain,data}/.gitkeep`
- Create: `lib/features/chat/{presentation,application,domain,data}/.gitkeep`
- Create: `lib/features/progress/{presentation,application,domain,data}/.gitkeep`
- Create: `lib/features/settings/{presentation,application,domain,data}/.gitkeep`

**Interfaces:**
- Consumes: Task 3'ün ürettiği `lib/` dizini
- Produces: Sonraki fazların (F1: `lib/features/onboarding/`, F2: `lib/features/nutrition/` vb.) kod yazacağı klasör iskeleti

- [ ] **Step 1: Klasörleri ve .gitkeep dosyalarını oluştur**

Run (Git Bash):
```bash
cd "D:/spor ugulaması"
for feature in onboarding nutrition workout chat progress settings; do
  for layer in presentation application domain data; do
    mkdir -p "lib/features/$feature/$layer"
    touch "lib/features/$feature/$layer/.gitkeep"
  done
done
mkdir -p lib/core lib/shared
touch lib/core/.gitkeep lib/shared/.gitkeep
```
Expected: Komut hatasız tamamlanır.

- [ ] **Step 2: Klasör yapısının oluştuğunu doğrula**

Run: `find lib/features -type d | sort`
Expected: 6 feature × 4 layer = 24 klasör satırı listelenir (`lib/features/chat/application`, `lib/features/chat/data`, ... `lib/features/workout/presentation`).

- [ ] **Step 3: Analyze'ın hâlâ temiz olduğunu doğrula**

Run: `flutter analyze`
Expected: `No issues found!` (boş klasörler analyze'ı etkilemez)

- [ ] **Step 4: Commit**

```bash
cd "D:/spor ugulaması"
git -c safe.directory="D:/spor ugulaması" add -A
git -c safe.directory="D:/spor ugulaması" commit -m "$(cat <<'EOF'
Add feature-first folder skeleton for E1-E9 epics

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01PDcH7HBWBdAAUWM5Tnp7GU
EOF
)"
```
Expected: Commit başarılı.

---

## Task 5: Riverpod, Supabase, go_router, dotenv bağımlılıklarını ekle

**Files:**
- Modify: `pubspec.yaml`

**Interfaces:**
- Consumes: Task 3'ün ürettiği `pubspec.yaml`
- Produces: `flutter_riverpod`, `supabase_flutter`, `go_router`, `flutter_dotenv` paketleri — Task 6+ bunları import edecek

- [ ] **Step 1: Bağımlılıkları ekle**

Run:
```bash
cd "D:/spor ugulaması"
flutter pub add flutter_riverpod supabase_flutter go_router flutter_dotenv
```
Expected: Her paket için `Changed X to Y in pubspec.yaml!` mesajı, hata yok.

- [ ] **Step 2: pub get'in temiz çalıştığını doğrula**

Run: `flutter pub get`
Expected: `Got dependencies!` (çakışma/uyarı yok)

- [ ] **Step 3: analyze'ın hâlâ temiz olduğunu doğrula**

Run: `flutter analyze`
Expected: `No issues found!`

- [ ] **Step 4: Commit**

```bash
cd "D:/spor ugulaması"
git -c safe.directory="D:/spor ugulaması" add pubspec.yaml pubspec.lock
git -c safe.directory="D:/spor ugulaması" commit -m "$(cat <<'EOF'
Add Riverpod, Supabase, go_router, and dotenv dependencies

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01PDcH7HBWBdAAUWM5Tnp7GU
EOF
)"
```
Expected: Commit başarılı.

---

## Task 6: .env yönetimi ve Supabase client iskeleti

**Files:**
- Create: `.env.example`
- Create: `lib/core/env.dart`
- Create: `lib/core/supabase_client.dart`
- Modify: `.gitignore` (`.env` satırı eklenir)
- Modify: `lib/main.dart`
- Modify: `test/widget_test.dart`

**Interfaces:**
- Consumes: Task 5'in eklediği `flutter_dotenv`, `supabase_flutter` paketleri
- Produces:
  - `Env.supabaseUrl` (String getter), `Env.supabaseAnonKey` (String getter) — F1+ bu değerleri kullanacak
  - `AppSupabase.init()` (Future<void>, main() içinde çağrılır), `AppSupabase.client` (SupabaseClient getter) — F1+ veritabanı/auth işlemleri bunu kullanacak
  - `SporTakipApp` (StatelessWidget, `main.dart`'taki kök widget) — F1+ router/tema burada genişletilecek

- [ ] **Step 1: .gitignore'a .env ekle**

`.gitignore` dosyasının sonuna ekle:
```
# Environment secrets
.env
```

- [ ] **Step 2: .env.example oluştur**

`.env.example` içeriği:
```
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
```

- [ ] **Step 3: Geçici .env oluştur (Task 8'de gerçek değerlerle güncellenecek)**

`.env` içeriği (placeholder, henüz gerçek Supabase projesi yok):
```
SUPABASE_URL=https://placeholder.supabase.co
SUPABASE_ANON_KEY=placeholder-anon-key
```

- [ ] **Step 4: lib/core/env.dart yaz**

```dart
import 'package:flutter_dotenv/flutter_dotenv.dart';

class Env {
  static String get supabaseUrl => dotenv.get('SUPABASE_URL');
  static String get supabaseAnonKey => dotenv.get('SUPABASE_ANON_KEY');
}
```

- [ ] **Step 5: lib/core/supabase_client.dart yaz**

```dart
import 'package:supabase_flutter/supabase_flutter.dart';

import 'env.dart';

class AppSupabase {
  static Future<void> init() {
    return Supabase.initialize(
      url: Env.supabaseUrl,
      anonKey: Env.supabaseAnonKey,
    );
  }

  static SupabaseClient get client => Supabase.instance.client;
}
```

- [ ] **Step 6: lib/main.dart'ı güncelle**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_dotenv/flutter_dotenv.dart';

import 'core/supabase_client.dart';

Future<void> main() async {
  WidgetsFlutterBinding.ensureInitialized();
  await dotenv.load(fileName: '.env');
  await AppSupabase.init();
  runApp(const SporTakipApp());
}

class SporTakipApp extends StatelessWidget {
  const SporTakipApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Spor Takip',
      home: const Scaffold(
        body: Center(child: Text('F0 iskeleti hazır')),
      ),
    );
  }
}
```

- [ ] **Step 7: pubspec.yaml'a .env'i asset olarak ekle**

`pubspec.yaml` içindeki `flutter:` bölümüne (yoksa oluştur):
```yaml
flutter:
  uses-material-design: true
  assets:
    - .env
```

- [ ] **Step 8: test/widget_test.dart'ı yeni kök widget'a göre güncelle**

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:spor_takip/main.dart';

void main() {
  testWidgets('App boots and shows F0 placeholder text', (tester) async {
    await tester.pumpWidget(const SporTakipApp());

    expect(find.text('F0 iskeleti hazır'), findsOneWidget);
  });
}
```

Not: Bu test `SporTakipApp` widget'ını doğrudan `pumpWidget` ile monte eder, `main()`'i çağırmaz — bu yüzden `dotenv.load` veya `AppSupabase.init()` test sırasında hiç tetiklenmez, ağ/plugin bağımlılığı olmadan çalışır.

- [ ] **Step 9: Testin geçtiğini doğrula**

Run: `flutter test`
Expected: `00:0X +1: All tests passed!`

- [ ] **Step 10: analyze'ın temiz olduğunu doğrula**

Run: `flutter analyze`
Expected: `No issues found!`

- [ ] **Step 11: .env'in git tarafından yok sayıldığını doğrula**

Run: `git -c safe.directory="D:/spor ugulaması" status --short`
Expected: `.env` listede **görünmemeli** (ignore edilmiş); `.env.example`, `.gitignore`, `lib/core/env.dart`, `lib/core/supabase_client.dart`, değişen `lib/main.dart`, `test/widget_test.dart`, `pubspec.yaml` görünmeli.

- [ ] **Step 12: Commit**

```bash
cd "D:/spor ugulaması"
git -c safe.directory="D:/spor ugulaması" add .gitignore .env.example pubspec.yaml lib/core/env.dart lib/core/supabase_client.dart lib/main.dart test/widget_test.dart
git -c safe.directory="D:/spor ugulaması" commit -m "$(cat <<'EOF'
Add .env-based config and Supabase client skeleton

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01PDcH7HBWBdAAUWM5Tnp7GU
EOF
)"
```
Expected: Commit başarılı; `.env` commit edilmemiş olmalı (Step 11'de doğrulandı).

---

## Task 7: GitHub Actions CI pipeline'ı

**Files:**
- Create: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: Task 2'de doğrulanan Flutter sürüm numarası
- Produces: Her push/PR'da çalışan `analyze-and-test` job'ı

- [ ] **Step 1: Workflow dizinini oluştur ve dosyayı yaz**

`.github/workflows/ci.yml` (Task 2 Step 2'de not edilen gerçek Flutter sürümünü `flutter-version` alanına yaz — örnek aşağıda `3.47.4` placeholder'ı gerçek doğrulanmış sürümle değiştirilecek):

```yaml
name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  analyze-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.47.4"
          channel: "stable"

      - name: Create placeholder .env for CI
        run: |
          echo "SUPABASE_URL=https://placeholder.supabase.co" > .env
          echo "SUPABASE_ANON_KEY=placeholder-anon-key" >> .env

      - run: flutter pub get
      - run: flutter analyze
      - run: flutter test
```

Not: CI'da gerçek Supabase kimlik bilgileri yok — `.env` placeholder değerlerle oluşturulur, bu F0'daki testler (Task 6 Step 8) zaten Supabase'e dokunmadığı için yeterlidir.

- [ ] **Step 2: YAML söz diziminin geçerli olduğunu doğrula**

Run: `powershell -Command "Get-Content '.github/workflows/ci.yml' | Out-String | python -c \"import sys,yaml; yaml.safe_load(sys.stdin)\" 2>&1"` (python/yaml yoksa: dosyayı görsel olarak indent hatası için gözden geçir)
Expected: Hata çıktısı yok (geçerli YAML).

- [ ] **Step 3: Commit**

```bash
cd "D:/spor ugulaması"
git -c safe.directory="D:/spor ugulaması" add .github/workflows/ci.yml
git -c safe.directory="D:/spor ugulaması" commit -m "$(cat <<'EOF'
Add GitHub Actions CI for flutter analyze and test

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01PDcH7HBWBdAAUWM5Tnp7GU
EOF
)"
```
Expected: Commit başarılı.

---

## Task 8: Supabase projesini kullanıcıyla birlikte kur ve bağla

**Files:**
- Modify: `.env` (gerçek değerlerle; commit edilmez)

**Interfaces:**
- Consumes: Task 6'nın `Env.supabaseUrl`/`Env.supabaseAnonKey` getter'ları (kod değişmez, sadece `.env` içeriği değişir)
- Produces: Gerçek bir Supabase projesine bağlı çalışma ortamı (F1'in tablo/RLS kurulumu bunun üzerine gelecek)

- [ ] **Step 1: Kullanıcıdan Supabase projesi oluşturmasını iste**

Kullanıcıya şunu ilet: "https://supabase.com üzerinden yeni bir proje oluştur (örn. proje adı: spor-takip), oluşturduktan sonra Project Settings → API sayfasından **Project URL** ve **anon public key**'i paylaş."

- [ ] **Step 2: Paylaşılan bilgilerle .env'i güncelle**

`.env` dosyasını gerçek değerlerle güncelle:
```
SUPABASE_URL=<kullanıcının paylaştığı URL>
SUPABASE_ANON_KEY=<kullanıcının paylaştığı anon key>
```

- [ ] **Step 3: .env'in hâlâ git tarafından yok sayıldığını doğrula**

Run: `git -c safe.directory="D:/spor ugulaması" status --short`
Expected: `.env` listede görünmemeli.

- [ ] **Step 4: analyze ve test'in hâlâ temiz olduğunu doğrula**

Run: `flutter analyze && flutter test`
Expected: `No issues found!` ve `All tests passed!` (Task 6 Step 8'deki test .env'e dokunmadığı için gerçek değerlerden etkilenmez)

- [ ] **Step 5: (Opsiyonel, kullanıcı bir Android cihaz/emülatör bağlıysa) Manuel smoke test**

Run: `flutter run` (bağlı bir cihaz/emülatör varsa)
Expected: Uygulama açılır, "F0 iskeleti hazır" yazısı görünür, konsolda Supabase bağlantı hatası (örn. `AuthException`, `SocketException`) **olmamalı**. Cihaz/emülatör yoksa bu adım atlanır, F1'de tekrar ele alınır.

- [ ] **Step 6: Commit yok**

`.env` gitignore'lu olduğu için bu task'ta commit edilecek bir şey yok — bu normal ve beklenen davranıştır.

---

## Task 9: F0'ın tamamını doğrula ve kapat

**Files:** Yok

**Interfaces:**
- Consumes: Task 1-8'in tüm ürünleri
- Produces: F0'ın tamamlandığını gösteren yeşil doğrulama + `PLAN.md` değişiklik günlüğü güncellemesi

- [ ] **Step 1: Tüm proje için analyze çalıştır**

Run: `cd "D:/spor ugulaması" && flutter analyze`
Expected: `No issues found!`

- [ ] **Step 2: Tüm testleri çalıştır**

Run: `cd "D:/spor ugulaması" && flutter test`
Expected: `All tests passed!`

- [ ] **Step 3: Git durumunun temiz olduğunu doğrula**

Run: `git -c safe.directory="D:/spor ugulaması" status`
Expected: `.env` hariç her şey commit edilmiş (`nothing to commit, working tree clean` veya sadece ignore edilen dosyalar).

- [ ] **Step 4: PLAN.md'nin Değişiklik Günlüğü'nü güncelle**

`PLAN.md` içindeki `## 10. Değişiklik Günlüğü` tablosuna satır ekle:
```
| 2026-09-16 | F0 tamamlandı: Flutter+Supabase ortamı, feature-first iskelet, CI kuruldu. |
```

- [ ] **Step 5: Commit**

```bash
cd "D:/spor ugulaması"
git -c safe.directory="D:/spor ugulaması" add PLAN.md
git -c safe.directory="D:/spor ugulaması" commit -m "$(cat <<'EOF'
Mark F0 complete in PLAN.md changelog

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01PDcH7HBWBdAAUWM5Tnp7GU
EOF
)"
```
Expected: Commit başarılı. F0 tamamlanmıştır; sıradaki adım F1 (Auth + Onboarding) için yeni bir brainstorming/spec döngüsüdür.
