# Only the Supabase anon/publishable key belongs here. Never put a
# service_role key in this file — it is bundled as a pubspec asset and
# would ship inside the app binary.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
